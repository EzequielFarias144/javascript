/*const getPrice = (total) => /*<= funcao de seta {
    const discount = 0.10
    return total - (discount * total);
};
console.log(getPrice(400));*/

/*function soma(num1, num2) {
    const so =  num1 + num2
    console.log(so);
};*/

/*soma(2, 10);

soma = (num1, num2) => {
    return console.log(num1 + num2)
};

soma(4, 6);

getPrice = (total) => {
    const discount = 0.10
    return total - (discount * total);
};

getPrice(400);*/

function hello() {
    const string1 = "o dia foi bom!"
    return console.log(string1);
};

hello();