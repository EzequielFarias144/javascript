let cores = ['ciano', 'verde', 'amarelo'];

const [ciano, verde, amarelo] = cores;

const mensagem = `No sistema de cores CYMK, a cor ${verde} é formada pela adição das cores ${amarelo} e ${ciano}.`;

console.log(mensagem);